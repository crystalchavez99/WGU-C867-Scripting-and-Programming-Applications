#include <iostream>
#include <cmath>
using namespace std;

int main() {
   double x;
   double y;
   double z;

   x = 2.0;
   y = 3.0;
   x = 9.0;
   z = pow(sqrt(x) + sqrt(x), 2.0);
   cout << z;

   return 0;
}
